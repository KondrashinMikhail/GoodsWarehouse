spring:
  datasource:
    username: goods-warehouse
    password: 200214
    url: jdbc:postgresql://postgres:5432/goods-warehouse
  jpa:
    hibernate:
      ddl-auto: validate
app:
  scheduling:
    enabled: true
minio:
  url: ${MINIO_ENDPOINT:http://minio:9000}
