spring:
  kafka:
    bootstrap-servers: ${KAFKA_SERVERS:PLAINTEXT_HOST://localhost:9091}
  datasource:
    url: jdbc:h2:mem:testdb
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: validate
app:
  kafka:
    enabled: true
  scheduling:
    enabled: false
minio:
  url: ${MINIO_ENDPOINT:http://localhost:9000}
  access-key: admin
  secret-key: password